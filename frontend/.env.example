# Leave empty for same-origin deployments where /api is proxied to the backend.
# For separate frontend/backend hosts, use the public backend origin without a trailing slash.
VITE_API_BASE_URL=https://api.example.com